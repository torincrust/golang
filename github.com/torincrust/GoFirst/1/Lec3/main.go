// счтываение данных
package main

import (
	"fmt"
	//"os"
	"math"
)

func main() {
	/* var (
		age  int
		name string
	)
	fmt.Scan(&age, &name) // чирез амперсанд передается указателья куда нужна записать считанные значения

	fmt.Printf("My name is: %s\nmy age is: %d\n", name, age)

	// для ручного использования потока ввода

	fmt.Fscan(os.Stdin, &age)
	fmt.Println("New age:", age) */

	// задачи дз
	// номер 1 пропуск
	/* var (
		fName string
		sName string
		age   int
	)
	fmt.Scan(&fName, &sName, &age)
	fmt.Printf("Имя: %s Фамилия: %s, Возраст: %d", fName, sName, age) */

	/* // периметра задача 2
	var (
		a int
		b int
	)
	fmt.Scan(&a, a)
	fmt.Scan(&b, b)
	var pl = a * b
	var per = (a + b) * 2

	fmt.Printf("Площадь прямоугольника: %d\nПериметр прямоугольника: %d", pl, per)
	*/


